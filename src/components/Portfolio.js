import React from 'react';
import { SectionDesc, SectionHeader, SectionWrapper } from './Styled';

const Portfolio = () => (
  <SectionWrapper bg="#f4f3f3">
    <SectionHeader>Portfolio</SectionHeader>
    <SectionDesc>Unless stated otherwise, all the back-end and front-end parts were completely done by me.</SectionDesc>
  </SectionWrapper>
);

export default Portfolio;
